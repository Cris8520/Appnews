import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appnews-master',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
